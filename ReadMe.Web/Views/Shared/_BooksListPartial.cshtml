@model IEnumerable<ReadMe.Web.Models.Books.BookShortViewModel>

<div class="list-group">
    @foreach (var book in @Model)
    {
        Html.RenderPartial("_BookShortPartial", book);
    }
</div>